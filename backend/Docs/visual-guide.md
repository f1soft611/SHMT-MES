# Scheduler Date Range Feature - Visual Guide

## Feature Flow Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                    User Interface (React)                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │         Scheduler List (DataGrid)                      │    │
│  │                                                         │    │
│  │  ┌──────────┬────────────┬──────────┬──────────────┐  │    │
│  │  │ ID │ Name  │ CRON     │ Status   │ Actions     │  │    │
│  │  ├──────────┼────────────┼──────────┼──────────────┤  │    │
│  │  │ 1  │ 사원정보│ 0 2 * * *│ Active   │ [▶] [✎] [🗑]│  │    │
│  │  │ 2  │ 거래처  │ 0 2 * * *│ Active   │ [▶] [✎] [🗑]│  │    │
│  │  │ 3  │ 생산의뢰│ 0 2 * * *│ Active   │ [▶] [✎] [🗑]│  │    │
│  │  └──────────┴────────────┴──────────┴──────────────┘  │    │
│  │                           Click [▶]                     │    │
│  └────────────────────┬───────────────────────────────────┘    │
│                       │                                          │
│                       ▼                                          │
│  ┌────────────────────────────────────────────────────────┐    │
│  │     Date Range Selection Dialog                        │    │
│  │                                                         │    │
│  │  스케쥴러: 사원정보 연동                                │    │
│  │                                                         │    │
│  │  시작 날짜: [2025-01-01]  📅                          │    │
│  │  종료 날짜: [2025-01-31]  📅                          │    │
│  │                                                         │    │
│  │              [취소]  [실행]                            │    │
│  └────────────────────────────────────────────────────────┘    │
│                       Click [실행]                              │
└───────────────────────┬─────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────────┐
│                    REST API (Spring Boot)                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  POST /api/schedulers/1/execute                                 │
│       ?fromDate=2025-01-01                                      │
│       &toDate=2025-01-31                                        │
│                                                                  │
│  ┌──────────────────────────────────────┐                      │
│  │  SchedulerConfigApiController        │                      │
│  │  └─► executeScheduler()              │                      │
│  └──────────────────┬───────────────────┘                      │
│                     │                                            │
│                     ▼                                            │
│  ┌──────────────────────────────────────┐                      │
│  │  SchedulerConfigService              │                      │
│  │  └─► executeSchedulerManually()      │                      │
│  └──────────────────┬───────────────────┘                      │
│                     │                                            │
│                     ▼                                            │
│  ┌──────────────────────────────────────┐                      │
│  │  DynamicSchedulerService             │                      │
│  │  • Set dates (or use today)          │                      │
│  │  • Create task runnable              │                      │
│  │  • Schedule execution                │                      │
│  └──────────────────┬───────────────────┘                      │
└─────────────────────┼───────────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────────┐
│                Business Logic (Services)                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────────────────────────────────────────┐      │
│  │  ErpToMesInterfaceService                            │      │
│  │                                                       │      │
│  │  executeUserInterface(fromDate, toDate)              │      │
│  │  ├─► syncUsers(fromDate, toDate)                     │      │
│  │  │   └─► selectErpEmployees(fromDate, toDate)       │      │
│  │  │                                                    │      │
│  │  executeCustInterface(fromDate, toDate)              │      │
│  │  ├─► syncCusts(fromDate, toDate)                     │      │
│  │  │   └─► selectErpCustomers(fromDate, toDate)       │      │
│  │  │                                                    │      │
│  │  executeProdReqInterface(fromDate, toDate)           │      │
│  │  └─► syncProductionRequests(fromDate, toDate)        │      │
│  │      └─► selectErpProductionRequests(fromDate, toDate)│     │
│  └──────────────────┬───────────────────────────────────┘      │
└─────────────────────┼───────────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Database (SQL Server)                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────────────────────────────────────────┐      │
│  │ SELECT ... FROM SHM_IF_VIEW_TDAEmp                   │      │
│  │ WHERE TypeSeq = 3031001                              │      │
│  │   AND CONVERT(VARCHAR(10), LastDateTime, 120)       │      │
│  │       BETWEEN '2025-01-01' AND '2025-01-31'          │      │
│  └──────────────────────────────────────────────────────┘      │
│                                                                  │
│  ┌──────────────────────────────────────────────────────┐      │
│  │ SELECT ... FROM SHM_IF_VIEW_TDACust                  │      │
│  │ WHERE SMCustStatus = '2004001'                       │      │
│  │   AND CONVERT(VARCHAR(10), LastDateTime, 120)       │      │
│  │       BETWEEN '2025-01-01' AND '2025-01-31'          │      │
│  └──────────────────────────────────────────────────────┘      │
│                                                                  │
│  ┌──────────────────────────────────────────────────────┐      │
│  │ SELECT ... FROM SHM_IF_VIEW_ProdReq                  │      │
│  │ WHERE ReqDate BETWEEN '2025-01-01' AND '2025-01-31'  │      │
│  └──────────────────────────────────────────────────────┘      │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

## Date Handling Logic

### Manual Execution (User-Initiated)

```
User Action
    │
    ├─► Select Date Range in Dialog
    │       │
    │       ├─► From: 2025-01-01
    │       └─► To: 2025-01-31
    │
    └─► Click Execute
            │
            └─► API Call with dates
                    │
                    └─► Filter data by date range
```

### Automatic Execution (CRON-Scheduled)

```
CRON Trigger (e.g., 0 2 * * *)
    │
    └─► scheduleTask() called
            │
            ├─► today = new SimpleDateFormat("yyyy-MM-dd").format(new Date())
            │       │
            │       └─► "2025-11-19"
            │
            └─► createTaskRunnable(config, today, today)
                    │
                    └─► Filter data by today's date only
```

## Component Interaction

```
┌─────────────────────────────────────────────────────────┐
│                  DateRangeDialog.tsx                     │
│                                                          │
│  State:                                                  │
│  • fromDate: string (default: today)                    │
│  • toDate: string (default: today)                      │
│                                                          │
│  Methods:                                                │
│  • handleConfirm() → onConfirm(fromDate, toDate)        │
│  • handleClose() → reset dates, onClose()               │
└─────────────────┬───────────────────────────────────────┘
                  │
                  │ onConfirm callback
                  ▼
┌─────────────────────────────────────────────────────────┐
│                 SchedulerList.tsx                        │
│                                                          │
│  State:                                                  │
│  • dateRangeDialogOpen: boolean                         │
│  • selectedScheduler: { id, name } | null               │
│                                                          │
│  Methods:                                                │
│  • handleExecuteClick(id, name)                         │
│      → setSelectedScheduler({ id, name })               │
│      → setDateRangeDialogOpen(true)                     │
│                                                          │
│  • handleDateRangeConfirm(from, to)                     │
│      → executeMutation.mutate({                         │
│           schedulerId, fromDate, toDate                 │
│        })                                                │
└─────────────────┬───────────────────────────────────────┘
                  │
                  │ mutation
                  ▼
┌─────────────────────────────────────────────────────────┐
│              schedulerService.ts                         │
│                                                          │
│  async executeScheduler(                                │
│    schedulerId: number,                                 │
│    fromDate?: string,                                   │
│    toDate?: string                                      │
│  ): Promise<any> {                                      │
│    const params: any = {};                              │
│    if (fromDate) params.fromDate = fromDate;            │
│    if (toDate) params.toDate = toDate;                  │
│                                                          │
│    return api.post(                                     │
│      `/api/schedulers/${schedulerId}/execute`,          │
│      null,                                              │
│      { params }                                         │
│    );                                                    │
│  }                                                       │
└─────────────────────────────────────────────────────────┘
```

## Backend Method Call Chain

```
Controller
  │
  └─► SchedulerConfigApiController.executeScheduler()
          │ Parameters: schedulerId, fromDate?, toDate?
          │
          └─► SchedulerConfigService.executeSchedulerManually()
                  │ Parameters: schedulerId, fromDate, toDate
                  │
                  └─► DynamicSchedulerService.executeSchedulerManually()
                          │ • Set dates (null → today)
                          │ • Create task runnable
                          │
                          └─► createTaskRunnable()
                                  │
                                  └─► executeJob()
                                          │ • Use reflection
                                          │ • Find method with dates
                                          │
                                          └─► ErpToMesInterfaceService.executeXXXInterface()
                                                  │ Parameters: fromDate, toDate
                                                  │
                                                  └─► syncXXX()
                                                          │
                                                          └─► selectErpXXX()
                                                                  │ SQL with date filter
                                                                  │
                                                                  └─► Database Query
```

## Date Format Examples

### Frontend (JavaScript Date)
```javascript
const today = new Date().toISOString().split('T')[0];
// Result: "2025-11-19"
```

### Backend (Java SimpleDateFormat)
```java
String today = new SimpleDateFormat("yyyy-MM-dd").format(new Date());
// Result: "2025-11-19"
```

### SQL (MSSQL CONVERT)
```sql
CONVERT(VARCHAR(10), LastDateTime, 120) BETWEEN '2025-01-01' AND '2025-01-31'
-- LastDateTime: 2025-01-15 14:30:00.000
-- Converted: "2025-01-15"
```

## Key Decision Points

### 1. Date Parameter Handling
```
Input: fromDate=null, toDate=null
   ↓
Decision: Are dates provided?
   ├─► NO: Use today's date
   │       fromDate = today
   │       toDate = today
   │
   └─► YES: Use provided dates
           fromDate = provided
           toDate = provided
```

### 2. Method Invocation Strategy
```
Find method in service
   ↓
Try: method(String, String)  ← Date parameters
   ├─► SUCCESS: Call with dates
   │
   └─► FAIL: Try method()  ← No parameters
           ├─► SUCCESS: Call without dates
           │
           └─► FAIL: Throw exception
```

## Statistics

### Lines Changed
- **Total**: 860 lines added, 67 lines removed
- **Backend**: ~250 lines modified/added
- **Frontend**: ~150 lines modified/added
- **Documentation**: ~600 lines added

### Files Modified
- **Backend**: 7 Java files
- **Frontend**: 3 TypeScript/TSX files
- **Documentation**: 2 Markdown files

### Schedulers Enhanced
- ✅ User Interface (사원정보)
- ✅ Customer Interface (거래처)
- ✅ Production Request Interface (생산의뢰)
- ✅ All Interface (전체 인터페이스)

## Success Criteria Met

| Requirement | Status | Notes |
|-------------|--------|-------|
| Manual execution with date range | ✅ | DateRangeDialog implemented |
| Automatic execution with today | ✅ | DynamicSchedulerServiceImpl modified |
| Apply to all schedulers | ✅ | 3 schedulers enhanced |
| Popup for date selection | ✅ | Material-UI Dialog |
| Backend date filtering | ✅ | SQL queries modified |
| Frontend integration | ✅ | React components updated |
| Documentation | ✅ | Comprehensive guides created |
