# 로컬 개발 환경 설정 가이드

## 📋 개요

이 문서는 SHMT-MES 프로젝트를 로컬 환경에서 개발하기 위한 설정 방법을 안내합니다.

---

## 🔧 사전 요구사항

### 필수 소프트웨어
- **Java**: JDK 11 이상
- **Node.js**: 16.x 이상
- **Maven**: 3.6 이상
- **데이터베이스**: MS SQL Server (또는 MySQL, Oracle 등)
- **Git**: 최신 버전

---

## 🚀 백엔드 설정

### 1. 프로젝트 클론

```bash
git clone https://github.com/f1soft611/SHMT-MES.git
cd SHMT-MES/backend
```

### 2. 데이터베이스 설정

#### 데이터베이스 생성
```sql
-- MS SQL Server 예시
CREATE DATABASE iPlusMES_SHMT;
```

#### 스키마 생성
```bash
cd DATABASE
# 사용하는 데이터베이스에 맞는 DDL 파일 실행
# MS SQL: all_sht_ddl_mssql.sql
# MySQL: all_sht_ddl_mysql.sql
```

### 3. 애플리케이션 설정

#### application-local.properties 생성

```bash
cd src/main/resources
cp application.properties.example application-local.properties
```

#### application-local.properties 수정

**중요**: 이 파일은 Git에 커밋되지 않습니다!

```properties
# 운영서버 타입
Globals.OsType=WINDOWS

# DB서버 타입 선택 (mssql, mysql, oracle 등)
Globals.DbType=mssql

# MS SQL Server 설정 (실제 값으로 변경)
Globals.mssql.DriverClassName=net.sf.log4jdbc.DriverSpy
Globals.mssql.Url=jdbc:log4jdbc:sqlserver://localhost:1433;databaseName=iPlusMES_SHMT;encrypt=true;trustServerCertificate=true
Globals.mssql.UserName=sa
Globals.mssql.Password=YourPassword123!

# ERP 데이터베이스 설정 (ERP 연동이 필요한 경우)
Globals.erp.DriverClassName=net.sf.log4jdbc.DriverSpy
Globals.erp.Url=jdbc:log4jdbc:sqlserver://your-erp-host:1433;databaseName=ERP_DB;encrypt=true;trustServerCertificate=true
Globals.erp.UserName=erp_user
Globals.erp.Password=ErpPassword123!

# JWT Secret Key (강력한 랜덤 키 생성 권장)
# 생성 방법: openssl rand -base64 64
Globals.jwt.secret=your_strong_jwt_secret_key_here_minimum_32_characters

# 암호화 알고리즘 키 (기본값을 변경하세요)
Globals.crypto.algoritm=your_crypto_key_here

# CORS 허용 Origin
Globals.Allow.Origin=http://localhost:3000

# SNS 로그인 설정 (사용하는 경우)
Sns.naver.clientId=your_naver_client_id
Sns.naver.clientSecret=your_naver_client_secret
Sns.kakao.clientId=your_kakao_client_id
```

#### application.properties 수정

```properties
# local 프로파일 활성화
spring.profiles.active=local
```

### 4. 빌드 및 실행

```bash
# Maven 빌드
mvn clean install

# Spring Boot 실행
mvn spring-boot:run
```

서버가 `http://localhost:8080`에서 실행됩니다.

#### Swagger UI 접근
- URL: `http://localhost:8080/swagger-ui/index.html`

---

## 🎨 프론트엔드 설정

### 1. 디렉토리 이동

```bash
cd ../frontend
```

### 2. 의존성 설치

```bash
npm install
```

### 3. 환경 설정

#### .env.local 파일 생성

```bash
cp env.development .env.local
```

#### .env.local 파일 수정

**중요**: 이 파일은 Git에 커밋되지 않습니다!

```env
REACT_APP_API_BASE_URL=http://localhost:8080
REACT_APP_API_TIMEOUT=10000
REACT_APP_ENV=local
```

### 4. 개발 서버 실행

```bash
npm start
```

브라우저에서 `http://localhost:3000`이 자동으로 열립니다.

---

## 🧪 테스트 실행

### 백엔드 테스트

```bash
cd backend
mvn test
```

### 프론트엔드 테스트

```bash
cd frontend
npm test
```

---

## 🔍 문제 해결

### 백엔드 관련

#### 데이터베이스 연결 오류
- 데이터베이스 서버가 실행 중인지 확인
- `application-local.properties`의 접속 정보가 정확한지 확인
- 방화벽이 데이터베이스 포트를 차단하고 있지 않은지 확인

#### 포트 충돌 (8080)
```properties
# application-local.properties에서 포트 변경
server.port=8081
```

### 프론트엔드 관련

#### 포트 충돌 (3000)
```bash
# 다른 포트에서 실행
PORT=3001 npm start
```

#### API 연결 오류
- 백엔드 서버가 실행 중인지 확인
- `.env.local`의 `REACT_APP_API_BASE_URL`이 정확한지 확인
- 브라우저 콘솔에서 CORS 오류 확인

---

## 📝 개발 워크플로우

### 1. 새 기능 개발 시작

```bash
# 최신 코드 받기
git pull origin main

# 새 브랜치 생성
git checkout -b feature/new-feature-name
```

### 2. 개발 및 테스트

- 코드 작성
- 로컬에서 테스트
- 보안 체크리스트 확인 (SECURITY.md 참고)

### 3. 커밋 및 푸시

```bash
# 변경사항 확인
git status

# 민감한 정보가 포함되지 않았는지 확인!
git diff

# 스테이징 및 커밋
git add .
git commit -m "feat: 새 기능 추가"

# 푸시
git push origin feature/new-feature-name
```

### 4. Pull Request 생성

GitHub에서 Pull Request를 생성하고 코드 리뷰를 요청합니다.

---

## 🔐 보안 주의사항

### 절대 커밋하면 안 되는 파일:
- `application-local.properties`
- `.env.local`
- 실제 데이터베이스 접속 정보가 포함된 파일
- 회사 내부 문서 (엑셀, 워드, PDF 등)

### 커밋 전 체크리스트:
```bash
# 스테이징된 파일 확인
git status

# 변경 내용 상세 확인
git diff --staged

# .gitignore가 제대로 작동하는지 확인
git check-ignore -v application-local.properties
```

---

## 📞 지원

문제가 발생하거나 도움이 필요한 경우:
1. 프로젝트 Wiki 확인
2. 기존 Issue 검색
3. 새 Issue 생성 (단, 민감한 정보는 포함하지 말 것)

---

## 📚 추가 문서

- [프로젝트 README](../README.md)
- [백엔드 개발 가이드](./백엔드_개발_가이드.md)
- [프론트엔드 개발 가이드](./프론트엔드_개발_가이드.md)
- [통합 개발 워크플로우](./통합_개발_워크플로우.md)
- [보안 정책](../SECURITY.md)
